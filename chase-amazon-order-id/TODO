* grumble now *nothing* is happening at all
* run later but how do i know when? wait for element?
* get something barely working
** wait for something useful to be in the dom
https://gist.github.com/jwilson8767/db379026efcbd932f64382db4b02853e

document.querySelector('#activityContainersingleOVDAccountActivity')
** click hardcoded dom element to open details
document.querySelector("#transactionDetailIcon0").click()
** details
const dts=document.querySelectorAll('//dt//).map(e => e.textContent)
const dds=document.querySelector('//dd').map(e => e.textContent || e.querySelector('mds-link')['text'])
// Do some stuff!
document.querySelector("#flyoutClose")
// should probably wait for the element to be available again ...
** alert(the answer: date, description, amazon order number, amount)
* grab the orders
** will have to at least click on the right arrow
** and parse and click x
*** open
*** parse:
*** click x: document.querySelector("#flyoutClose").click()
** some notes
*** is it //*[@id="showHideAccountActivitysingleOVDAccountActivity"]/span
*** hmm thing with popup  //*[@id="transactionsingleOVDAccountActivity-0"]/td[5]
*** remarkably this worked to open document.querySelector("#transactionDetailIcon0").click()
*** can i get all td on page with name 'See details about this transaction' ?
*** does it matter whether shown or not?
*** hmm after popup xpath to the order id //*[@id="288567949202106241134437210613#20210613OrderNumber"]//a/span[1]

* search instead of just default activity since last statement
* maybe: action in popup, enabled only if there's something we can do
* nicer icons


* scratch

document.querySelector("#overviewPendingTooltip-trigger-icon").shadowRoot.querySelector("button")

On the popup this gets everything *except* the Amazon order number ... just because it isn't in text content I guess
 ... for that querySelector('mds-link')['text']
Note we should strip whitespace
const dts=$x("//dt")
const dds=$x("//dd")
dts.map(e => e.textContent)
dds.map(e => e.textContent || e.querySelector('mds-link')['text'])
* learned
** copy js path gets something usable in vanilla javascript


* HINT
https://github.com/djedi/chase-amazon/blob/master/ext/src/inject/inject.js